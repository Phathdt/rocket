// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: rocket/driver/v1/driver.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import type { handleUnaryCall, UntypedServiceImplementation } from '@grpc/grpc-js';
import { GrpcMethod, GrpcStreamMethod } from '@nestjs/microservices';
import { Observable } from 'rxjs';

export const protobufPackage = 'rocket.driver.v1';

export interface FindNearbyRequest {
  lat: number;
  lng: number;
  radiusKm: number;
  limit: number;
}

export interface NearbyDriver {
  driverId: string;
  distanceKm: number;
  name: string;
  vehiclePlate: string;
  vehicleModel: string;
}

export interface FindNearbyResponse {
  drivers: NearbyDriver[];
}

export interface AssignRequest {
  driverId: string;
}

export interface AssignResponse {
  ok: boolean;
}

export interface ReleaseRequest {
  driverId: string;
}

export interface ReleaseResponse {}

export const ROCKET_DRIVER_V1_PACKAGE_NAME = 'rocket.driver.v1';

function createBaseFindNearbyRequest(): FindNearbyRequest {
  return { lat: 0, lng: 0, radiusKm: 0, limit: 0 };
}

export const FindNearbyRequest: MessageFns<FindNearbyRequest> = {
  encode(message: FindNearbyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lat !== 0) {
      writer.uint32(9).double(message.lat);
    }
    if (message.lng !== 0) {
      writer.uint32(17).double(message.lng);
    }
    if (message.radiusKm !== 0) {
      writer.uint32(25).double(message.radiusKm);
    }
    if (message.limit !== 0) {
      writer.uint32(32).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FindNearbyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFindNearbyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.lat = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.lng = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.radiusKm = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseNearbyDriver(): NearbyDriver {
  return { driverId: '', distanceKm: 0, name: '', vehiclePlate: '', vehicleModel: '' };
}

export const NearbyDriver: MessageFns<NearbyDriver> = {
  encode(message: NearbyDriver, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.driverId !== '') {
      writer.uint32(10).string(message.driverId);
    }
    if (message.distanceKm !== 0) {
      writer.uint32(17).double(message.distanceKm);
    }
    if (message.name !== '') {
      writer.uint32(26).string(message.name);
    }
    if (message.vehiclePlate !== '') {
      writer.uint32(34).string(message.vehiclePlate);
    }
    if (message.vehicleModel !== '') {
      writer.uint32(42).string(message.vehicleModel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NearbyDriver {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNearbyDriver();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.driverId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.distanceKm = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.vehiclePlate = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.vehicleModel = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseFindNearbyResponse(): FindNearbyResponse {
  return { drivers: [] };
}

export const FindNearbyResponse: MessageFns<FindNearbyResponse> = {
  encode(message: FindNearbyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.drivers) {
      NearbyDriver.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FindNearbyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFindNearbyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.drivers.push(NearbyDriver.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAssignRequest(): AssignRequest {
  return { driverId: '' };
}

export const AssignRequest: MessageFns<AssignRequest> = {
  encode(message: AssignRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.driverId !== '') {
      writer.uint32(10).string(message.driverId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AssignRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssignRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.driverId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAssignResponse(): AssignResponse {
  return { ok: false };
}

export const AssignResponse: MessageFns<AssignResponse> = {
  encode(message: AssignResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AssignResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssignResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseReleaseRequest(): ReleaseRequest {
  return { driverId: '' };
}

export const ReleaseRequest: MessageFns<ReleaseRequest> = {
  encode(message: ReleaseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.driverId !== '') {
      writer.uint32(10).string(message.driverId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleaseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.driverId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseReleaseResponse(): ReleaseResponse {
  return {};
}

export const ReleaseResponse: MessageFns<ReleaseResponse> = {
  encode(_: ReleaseResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleaseResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

/**
 * Driver service gRPC contract.
 * Faithful replacement for the HTTP-based IDriverClient in trip-service.
 */

export interface DriverServiceClient {
  /** Find drivers near a coordinate, ordered by distance. */

  findNearby(request: FindNearbyRequest): Observable<FindNearbyResponse>;

  /** Atomically lock a driver to a trip. ok=false when already locked. */

  assign(request: AssignRequest): Observable<AssignResponse>;

  /** Release a previously assigned driver lock. Non-fatal / idempotent. */

  release(request: ReleaseRequest): Observable<ReleaseResponse>;
}

/**
 * Driver service gRPC contract.
 * Faithful replacement for the HTTP-based IDriverClient in trip-service.
 */

export interface DriverServiceController {
  /** Find drivers near a coordinate, ordered by distance. */

  findNearby(
    request: FindNearbyRequest,
  ): Promise<FindNearbyResponse> | Observable<FindNearbyResponse> | FindNearbyResponse;

  /** Atomically lock a driver to a trip. ok=false when already locked. */

  assign(
    request: AssignRequest,
  ): Promise<AssignResponse> | Observable<AssignResponse> | AssignResponse;

  /** Release a previously assigned driver lock. Non-fatal / idempotent. */

  release(
    request: ReleaseRequest,
  ): Promise<ReleaseResponse> | Observable<ReleaseResponse> | ReleaseResponse;
}

export function DriverServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ['findNearby', 'assign', 'release'];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod('DriverService', method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod('DriverService', method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const DRIVER_SERVICE_NAME = 'DriverService';

/**
 * Driver service gRPC contract.
 * Faithful replacement for the HTTP-based IDriverClient in trip-service.
 */
export type DriverServiceService = typeof DriverServiceService;
export const DriverServiceService = {
  /** Find drivers near a coordinate, ordered by distance. */
  findNearby: {
    path: '/rocket.driver.v1.DriverService/FindNearby' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: FindNearbyRequest): Buffer =>
      Buffer.from(FindNearbyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): FindNearbyRequest => FindNearbyRequest.decode(value),
    responseSerialize: (value: FindNearbyResponse): Buffer =>
      Buffer.from(FindNearbyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): FindNearbyResponse => FindNearbyResponse.decode(value),
  },
  /** Atomically lock a driver to a trip. ok=false when already locked. */
  assign: {
    path: '/rocket.driver.v1.DriverService/Assign' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AssignRequest): Buffer =>
      Buffer.from(AssignRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AssignRequest => AssignRequest.decode(value),
    responseSerialize: (value: AssignResponse): Buffer =>
      Buffer.from(AssignResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AssignResponse => AssignResponse.decode(value),
  },
  /** Release a previously assigned driver lock. Non-fatal / idempotent. */
  release: {
    path: '/rocket.driver.v1.DriverService/Release' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ReleaseRequest): Buffer =>
      Buffer.from(ReleaseRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReleaseRequest => ReleaseRequest.decode(value),
    responseSerialize: (value: ReleaseResponse): Buffer =>
      Buffer.from(ReleaseResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReleaseResponse => ReleaseResponse.decode(value),
  },
} as const;

export interface DriverServiceServer extends UntypedServiceImplementation {
  /** Find drivers near a coordinate, ordered by distance. */
  findNearby: handleUnaryCall<FindNearbyRequest, FindNearbyResponse>;
  /** Atomically lock a driver to a trip. ok=false when already locked. */
  assign: handleUnaryCall<AssignRequest, AssignResponse>;
  /** Release a previously assigned driver lock. Non-fatal / idempotent. */
  release: handleUnaryCall<ReleaseRequest, ReleaseResponse>;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
